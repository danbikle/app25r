%h1 Chart
This is a chart of
=params[:tkr]

%script(src="/d3.v3.min.js")

:javascript
  d3.json("/tkrs/data/IBM", function(error, rows) {
    if (error) throw error;
    var rows4chart = [];
    rows.forEach(function(row) {
      rows4chart.push({'x':row[0]});
      row;
    });
    'bye';
  });
